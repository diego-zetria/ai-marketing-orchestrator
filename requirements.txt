# Lambda dependencies for approval portal
# NOTE: boto3 is provided by the Lambda runtime, no need to bundle it
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.30.0
httpx>=0.28.0
pydantic>=2.0.0
pyjwt>=2.0.0
Pillow>=10.0.0
jinja2>=3.1.0
